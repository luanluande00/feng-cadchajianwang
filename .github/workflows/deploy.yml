# Netlify handles builds directly via its own CI.
# No need for GitHub Actions - just push to master and Netlify auto-deploys.
# 
# Build settings configured in Netlify dashboard:
#   Build command: npx prisma generate && npx next build
#   Publish directory: .next
#   Node version: 20
#
# See netlify.toml for configuration.
