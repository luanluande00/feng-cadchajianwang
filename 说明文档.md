# CAD插件商城 - 项目规划文档

## 一、项目概述

### 1.1 项目名称
CAD插件商城平台（CyberCAD Marketplace）

### 1.2 项目定位
一个赛博朋克风格的CAD插件交易平台，支持用户注册、插件发布、积分充值、插件下载等功能。

### 1.3 核心功能模块

| 模块 | 功能说明 | 优先级 |
|------|---------|--------|
| 用户系统 | 注册、登录、验证、子账号管理 | P0 |
| 积分系统 | 充值、消费、注册赠送、提现 | P0 |
| 插件商城 | 浏览、搜索、下载、发布 | P0 |
| 支付系统 | 集成支付接口，1积分=1元 | P0 |
| 管理后台 | 插件审核、用户管理、数据统计 | P1 |
| 开发者中心 | 插件上传、销售统计、收益提现 | P1 |

---

## 二、技术架构

### 2.1 技术栈选型

#### 前端技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| React | 18+ | UI框架 |
| Next.js | 14+ | SSR框架，支持SEO和部署 |
| Tailwind CSS | 3.4+ | 样式框架 |
| TypeScript | 5+ | 类型安全 |
| Framer Motion | - | 赛博朋克动效 |
| Zustand | - | 状态管理 |

#### 后端技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| Node.js | 20 LTS | 运行时环境 |
| Next.js API Routes | - | 服务端API |
| Prisma | 5+ | ORM框架 |
| PostgreSQL | 15+ | 数据库 |
| JWT | - | 身份验证 |
| Zod | - | 数据验证 |

#### 部署方案
| 服务 | 说明 |
|------|------|
| Netlify | 前后端统一部署（主方案） |
| GitHub | 代码托管 + Actions CI/CD |
| SQLite | 轻量数据库（/tmp/dev.db） |

### 2.2 项目结构
```
feng-cadchajianwang/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── (auth)/            # 认证相关页面
│   │   │   ├── login/         # 登录页
│   │   │   ├── register/      # 注册页
│   │   │   └── verify/        # 邮箱验证页
│   │   ├── (main)/            # 主要页面
│   │   │   ├── page.tsx       # 首页
│   │   │   ├── marketplace/   # 插件商城
│   │   │   ├── developer/     # 开发者中心
│   │   │   └── profile/       # 用户中心
│   │   ├── api/               # API路由
│   │   │   ├── auth/          # 认证API
│   │   │   ├── plugins/       # 插件API
│   │   │   ├── points/        # 积分API
│   │   │   └── payment/       # 支付API
│   │   └── layout.tsx         # 根布局
│   ├── components/            # 组件库
│   │   ├── ui/                # 基础UI组件
│   │   ├── layout/            # 布局组件
│   │   ├── cyberpunk/         # 赛博朋克特效组件
│   │   └── forms/             # 表单组件
│   ├── lib/                   # 工具库
│   │   ├── prisma.ts          # 数据库连接
│   │   ├── auth.ts            # 认证工具
│   │   ├── points.ts          # 积分逻辑
│   │   └── utils.ts           # 通用工具
│   ├── hooks/                 # 自定义Hooks
│   └── stores/                # 状态管理
├── prisma/
│   └── schema.prisma          # 数据库模型
├── public/                    # 静态资源
└── package.json
```

---

## 三、数据库设计

### 3.1 核心数据模型

```prisma
// 用户表
model User {
  id            String    @id @default(cuid())
  email         String    @unique
  password      String
  username      String    @unique
  avatar        String?
  points        Int       @default(0)      // 当前积分
  totalEarned   Int       @default(0)      // 累计赚取积分
  role          UserRole  @default(USER)
  isVerified    Boolean   @default(false)  // 邮箱验证状态
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  
  plugins       Plugin[]           // 发布的插件
  downloads     Download[]         // 下载记录
  transactions  Transaction[]      // 积分交易记录
  parentUserId  String?            // 父级用户ID（子账号）
  parentUser    User?     @relation("ParentChildren", fields: [parentUserId], references: [id])
  children      User[]    @relation("ParentChildren")
}

// 插件表
model Plugin {
  id            String      @id @default(cuid())
  name          String
  description   String
  coverImage    String
  fileUrl       String       // 插件文件地址
  price         Int          // 积分价格
  category      String
  status        PluginStatus @default(PENDING)  // 审核状态
  downloads     Int          @default(0)
  userId        String       // 发布者ID
  user          User         @relation(fields: [userId], references: [id])
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt
  
  downloadRecords Download[]
}

// 下载记录
model Download {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  pluginId  String
  plugin    Plugin   @relation(fields: [pluginId], references: [id])
  pointsSpent Int    // 消耗的积分
  createdAt DateTime @default(now())
}

// 交易记录
model Transaction {
  id          String        @id @default(cuid())
  userId      String
  user        User          @relation(fields: [userId], references: [id])
  type        TransactionType  // RECHARGE, SPEND, EARN, BONUS, WITHDRAW
  amount      Int              // 积分数量（正数为增加，负数为减少）
  description String
  createdAt   DateTime        @default(now())
}

// 充值订单
model PaymentOrder {
  id            String         @id @default(cuid())
  userId        String
  amount        Int            // 金额（元）
  points        Int            // 获得积分
  status        PaymentStatus  @default(PENDING)
  paymentUrl    String?        // 支付链接
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
}
```

### 3.2 枚举类型
```prisma
enum UserRole {
  USER
  DEVELOPER
  ADMIN
}

enum PluginStatus {
  PENDING    // 待审核
  APPROVED   // 已通过
  REJECTED   // 已拒绝
}

enum TransactionType {
  RECHARGE   // 充值
  SPEND      // 消费
  EARN       // 赚取
  BONUS      // 赠送（注册奖励等）
  WITHDRAW   // 提现
}

enum PaymentStatus {
  PENDING    // 待支付
  PAID       // 已支付
  CANCELLED  // 已取消
}
```

---

## 四、页面规划

### 4.1 页面结构图

```
CAD插件商城 (/)
├── 认证模块 (/auth)
│   ├── 登录 (/auth/login)
│   ├── 注册 (/auth/register)
│   └── 邮箱验证 (/auth/verify)
├── 首页 (/)
│   ├── 热门插件展示
│   ├── 最新插件
│   └── 推荐开发者
├── 插件商城 (/marketplace)
│   ├── 插件列表 (/marketplace/list)
│   ├── 插件详情 (/marketplace/plugin/[id])
│   └── 分类浏览 (/marketplace/category/[category])
├── 用户中心 (/profile)
│   ├── 个人信息 (/profile/info)
│   ├── 我的积分 (/profile/points)
│   ├── 下载历史 (/profile/downloads)
│   └── 充值中心 (/profile/recharge)
├── 开发者中心 (/developer)
│   ├── 发布插件 (/developer/publish)
│   ├── 我的插件 (/developer/plugins)
│   ├── 销售统计 (/developer/stats)
│   └── 收益提现 (/developer/withdraw)
└── 管理后台 (/admin)
    ├── 插件审核 (/admin/review)
    ├── 用户管理 (/admin/users)
    └── 数据统计 (/admin/stats)
```

### 4.2 URL映射表

| 页面 | URL | 父级 | 导航位置 | 优先级 |
|------|-----|------|---------|--------|
| 首页 | `/` | — | 头部导航 | 高 |
| 登录 | `/auth/login` | — | 头部按钮 | 高 |
| 注册 | `/auth/register` | — | 头部按钮 | 高 |
| 插件商城 | `/marketplace` | 首页 | 头部导航 | 高 |
| 插件详情 | `/marketplace/plugin/[id]` | 插件商城 | 内容链接 | 中 |
| 用户中心 | `/profile` | — | 用户菜单 | 高 |
| 积分充值 | `/profile/recharge` | 用户中心 | 侧边栏 | 高 |
| 开发者中心 | `/developer` | — | 头部导航 | 高 |
| 发布插件 | `/developer/publish` | 开发者中心 | 侧边栏 | 中 |
| 管理后台 | `/admin` | — | 隐藏入口 | 低 |

---

## 五、功能详细设计

### 5.1 用户系统

#### 5.1.1 注册流程
1. 用户填写邮箱、用户名、密码
2. 系统验证信息格式（邮箱格式、密码强度）
3. 创建用户账号，初始积分：100（注册奖励）
4. 发送验证邮件
5. 用户点击邮件链接完成验证

#### 5.1.2 登录流程
1. 用户输入邮箱/用户名 + 密码
2. 验证账号密码
3. 检查账号是否已验证邮箱
4. 生成JWT Token
5. 存储Token到HttpOnly Cookie

#### 5.1.3 子账号体系
- 主账号可创建子账号
- 子账号继承主账号部分权限
- 子账号发布的插件收益归主账号所有
- 支持子账号独立登录

### 5.2 积分系统

#### 5.2.1 积分规则
| 行为 | 积分变动 |
|------|---------|
| 注册奖励 | +100 |
| 充值 | 1元 = 1积分 |
| 下载付费插件 | 扣除插件定价积分 |
| 插件被下载 | 获得定价的70%积分 |
| 平台抽成 | 30% |
| 提现 | 100积分 = 1元（最低提现1000积分）

#### 5.2.2 积分流水
- 每次积分变动都记录Transaction
- 支持查看积分明细
- 防止积分为负数

### 5.3 支付系统

#### 5.3.1 支付流程
1. 用户选择充值金额
2. 创建支付订单
3. 调用支付接口生成支付链接
4. 用户完成支付
5. 支付回调更新订单状态
6. 增加用户积分

#### 5.3.2 支付方式
- 初期：支付宝当面付/微信Native支付
- 后期：可扩展其他支付方式

### 5.4 插件商城

#### 5.4.1 插件发布流程
1. 开发者填写插件信息（名称、描述、分类、价格）
2. 上传插件封面图和文件
3. 提交审核
4. 管理员审核通过后上架
5. 其他用户可浏览下载

#### 5.4.2 插件下载流程
1. 用户浏览插件详情
2. 检查积分是否充足
3. 扣除积分
4. 生成下载链接（带有效期）
5. 记录下载历史

### 5.5 赛博朋克视觉设计

#### 5.5.1 色彩方案
```css
/* 主色调 */
--neon-blue: #00f0ff;
--neon-pink: #ff006e;
--neon-purple: #b5179e;
--neon-yellow: #f9c80e;

/* 背景色 */
--bg-dark: #0a0a0f;
--bg-card: #16161e;
--bg-input: #1e1e2e;

/* 渐变 */
--gradient-primary: linear-gradient(135deg, #00f0ff 0%, #b5179e 100%);
--gradient-glow: linear-gradient(135deg, rgba(0, 240, 255, 0.1) 0%, rgba(181, 23, 158, 0.1) 100%);
```

#### 5.5.2 特效元素
- 霓虹发光边框效果
- 故障艺术（Glitch）动画
- 扫描线效果
- 粒子背景
- 赛博朋克字体（Orbitron, Rajdhani）

---

## 六、API接口设计

### 6.1 认证接口
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录 |
| POST | `/api/auth/logout` | 用户登出 |
| POST | `/api/auth/verify-email` | 发送验证邮件 |
| GET | `/api/auth/verify` | 验证邮箱链接 |
| GET | `/api/auth/me` | 获取当前用户信息 |

### 6.2 插件接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/plugins` | 获取插件列表 |
| GET | `/api/plugins/:id` | 获取插件详情 |
| POST | `/api/plugins` | 发布插件（需登录） |
| PUT | `/api/plugins/:id` | 更新插件（需登录） |
| DELETE | `/api/plugins/:id` | 删除插件（需登录） |
| POST | `/api/plugins/:id/download` | 下载插件（需登录+积分） |

### 6.3 积分接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/points/balance` | 查询积分余额 |
| GET | `/api/points/history` | 查询积分历史 |
| POST | `/api/points/transfer` | 积分转账 |

### 6.4 支付接口
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/payment/create` | 创建支付订单 |
| POST | `/api/payment/callback` | 支付回调 |
| GET | `/api/payment/status/:orderId` | 查询订单状态 |

---

## 七、开发计划

### 7.1 阶段一：项目初始化（第1天）
- [ ] 初始化Next.js项目
- [ ] 配置TypeScript
- [ ] 配置Tailwind CSS
- [ ] 配置Prisma和PostgreSQL
- [ ] 创建基础目录结构
- [ ] 配置ESLint和Prettier

### 7.2 阶段二：用户系统（第2-3天）
- [ ] 数据库模型设计
- [ ] 注册API开发
- [ ] 登录API开发
- [ ] JWT认证中间件
- [ ] 邮箱验证功能
- [ ] 注册页面开发
- [ ] 登录页面开发

### 7.3 阶段三：赛博朋克UI（第4-5天）
- [ ] 设计Token定义
- [ ] 基础组件库（Button, Input, Card等）
- [ ] 布局组件（Header, Footer, Sidebar）
- [ ] 赛博朋克特效组件
- [ ] 首页开发

### 7.4 阶段四：插件商城（第6-7天）
- [ ] 插件列表页面
- [ ] 插件详情页面
- [ ] 插件发布功能
- [ ] 文件上传功能
- [ ] 搜索和筛选功能

### 7.5 阶段五：积分与支付（第8-9天）
- [ ] 积分系统API
- [ ] 积分展示页面
- [ ] 支付接口集成
- [ ] 充值页面
- [ ] 支付回调处理

### 7.6 阶段六：用户中心（第10-11天）
- [ ] 个人信息管理
- [ ] 下载历史页面
- [ ] 开发者中心
- [ ] 销售统计
- [ ] 提现功能

### 7.7 阶段七：测试与部署（第12-14天）
- [ ] 功能测试
- [ ] 性能优化
- [ ] 部署到Vercel
- [ ] 数据库迁移
- [ ] 域名配置
- [ ] 监控设置

---

## 八、部署方案

### 8.1 实际部署信息

| 项目 | 值 |
|------|-----|
| 生产URL | https://cybercad-plugins.netlify.app |
| GitHub仓库 | https://github.com/luanluande00/feng-cadchajianwang |
| 部署平台 | Netlify |
| 数据库 | SQLite (file:/tmp/dev.db) |
| 数据库自动初始化 | 嵌套Proxy模式，首次查询自动建表 |
| 部署方式 | 本地构建 + netlify deploy --prod |

### 8.2 环境变量（Netlify）
```env
DATABASE_URL=file:/tmp/dev.db
BLOB_READ_WRITE_TOKEN=placeholder_token
NETLIFY_BLOBS_CONTEXT=dev
```

### 8.3 关键修复记录
1. **Vercel被墙**：`.vercel.app` 域名在国内被GFW封锁，迁移到Netlify
2. **Prisma二进制跨平台**：本地Windows构建+Netlify Lambda(Linux)运行，需同时包含rhel-openssl和linux-musl binaries
3. **DATABASE_URL不可达**：Netlify函数运行时环境变量不被传入，改用NODE_ENV判断
4. **Proxy只拦截顶层**：prisma.user.findUnique()绕过Proxy，改用嵌套Proxy拦截所有嵌套调用
5. **数据库自动初始化**：使用同一个PrismaClient实例完成表创建和查询

---

## 九、进度记录

| 日期 | 任务 | 状态 | 备注 |
|------|------|------|------|
| 2026-05-03 | 项目规划 | ✅ 已完成 | 创建项目文档，制定开发计划 |
| 2026-05-03 | 项目初始化 | ✅ 已完成 | Next.js+TypeScript+Tailwind配置完成 |
| 2026-05-03 | 数据库设计 | ✅ 已完成 | Prisma模型创建（User/Plugin/Download/Transaction/PaymentOrder） |
| 2026-05-03 | 核心工具库 | ✅ 已完成 | auth/points/email/validation/api-helpers/utils |
| 2026-05-03 | 认证系统API | ✅ 已完成 | register/login/logout/verify/me/verify-email |
| 2026-05-03 | 赛博朋克UI组件 | ✅ 已完成 | Button/Input/Card/GlitchText/ScanLine/Particles |
| 2026-05-03 | 布局组件 | ✅ 已完成 | Header/Footer |
| 2026-05-03 | 全局样式和布局 | ✅ 已完成 | globals.css/layout.tsx |
| 2026-05-03 | 认证页面 | ✅ 已完成 | login/register/verify |
| 2026-05-03 | 首页 | ✅ 已完成 | 展示统计数据、热门插件 |
| 2026-05-03 | 插件商城页面 | ✅ 已完成 | 列表、搜索、加载态 |
| 2026-05-03 | 插件API | ✅ 已完成 | 列表/详情/创建/更新/删除/下载 |
| 2026-05-03 | 积分API | ✅ 已完成 | 余额查询/历史记录/提现 |
| 2026-05-03 | 积分页面 | ✅ 已完成 | profile/points |
| 2026-05-03 | 支付系统 | ✅ 已完成 | payment API、充值页面 |
| 2026-05-03 | 用户中心 | ✅ 已完成 | profile/info/downloads/recharge |
| 2026-05-03 | 开发者中心 | ✅ 已完成 | developer/publish/plugins/stats/withdraw |
| 2026-05-03 | 管理后台 | ✅ 已完成 | admin/review/users/stats |
| 2026-05-03 | 404页面 | ✅ 已完成 | 自定义404页面 |
| 2026-05-03 | 构建验证 | ✅ 已完成 | npm run build 0错误 |
| 2026-05-03 | 动态路由修复 | ✅ 已完成 | 所有API添加dynamic=force-dynamic |
| 2026-05-03 | 404修复 | ✅ 已完成 | 所有页面路由已创建，无404 |
| 2026-05-03 | Vercel部署尝试 | ⚠️ 已废弃 | .vercel.app域名在国内被GFW封锁 |
| 2026-05-04 | Netlify部署成功 | ✅ 已完成 | https://cybercad-plugins.netlify.app |
| 2026-05-04 | 数据库自动初始化 | ✅ 已完成 | 嵌套Proxy+同一Client建表 |
| 2026-05-04 | 全站验证 | ✅ 已完成 | 所有页面200，注册/登录API正常 |
| 2026-05-04 | GitHub推送 | ✅ 已完成 | 代码已推送至luanluande00/feng-cadchajianwang |

---

## 十、风险与注意事项

### 10.1 技术风险
- 支付接口申请周期较长，需提前准备资质
- 文件存储需要考虑CDN加速
- 高并发下载需要限流处理

### 10.2 安全注意事项
- 密码必须bcrypt加密存储
- JWT Token设置合理过期时间
- 支付回调必须验证签名
- 文件上传需限制类型和大小
- 防SQL注入和XSS攻击

### 10.3 合规要求
- 用户协议和隐私政策
- ICP备案
- 支付接口需要企业资质
- 插件版权审核机制

---

## 十一、后续扩展规划

### 11.1 短期扩展
- 插件评论功能
- 开发者等级体系
- 积分排行榜
- 插件收藏功能

### 11.2 长期扩展
- 插件订阅制（月付/年付）
- 插件在线试用
- CAD在线编辑器集成
- API开放平台

---

*文档创建时间：2026-05-03*
*最后更新时间：2026-05-04*
